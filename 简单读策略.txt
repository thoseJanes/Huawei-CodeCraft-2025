每个请求来，就在文件对应块每个块加1，
来一个请求就在对应的三个磁盘上记录一下对应放在磁盘的哪个位置
对于每个磁盘，只看未来两步，只有两种策略
1. 跳一步，之后一直读
2. 连读两步 
收益为读取的磁盘块的记录到的物体块的请求数量之和
比较两种策略后取最好的那个策略执行
同时对读取了的文件块记录清0，并且遍历该文件请求链表，
每个请求用8位来记录每个请求已经读取的部分（比较文件大小最大为5） 
用位操作记录并判断是否读完，获取已经读完的请求

操作完后，刷新每个磁盘的请求位置，只保留当前块记录的请求数的最大的5个，（不清理的话会越来越大，计算策略2非常耗时）
